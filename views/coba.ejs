<% items.forEach(item => { %>

<%= item %>

<% }) %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <h1><strong>Quiz</strong></h1>
    </div>

    <div class="row">
        <% items.forEach(item => { %>
        <div class="box">
            <%= item.word %>
        </div>
        <% }) %>
    </div>
</div>